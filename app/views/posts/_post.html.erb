<div id="<%= dom_id post %>">
  <p>
    <strong>タイトル:</strong>
    <%= post.title %>
  </p>

  <p>
    <strong>内容:</strong>
    <%= post.body %>
  </p>

<% if post.tags.any? %>
  <p>
    <strong>タグ:</strong>
    <% post.tags.each do |tag| %>
      <%= link_to tag.name, tag_path(tag), class: "badge bg-info text-dark me-1" %> <%# 例: Bootstrapのバッジで表示 %>
    <% end %>
  </p>
<% else %>
  <p>タグは設定されていません。</p>
<% end %>

</div>
